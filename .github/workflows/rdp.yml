name: rdp
on: workflow_dispatch
jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0 -Force
          netsh advfirewall firewall set rule group="remote desktop" new enable=Yes
          net user runneradmin P@ssw0rd123!
          mkdir C:\Pangmatagalan -Force
      - run: |
          Invoke-WebRequest https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe -OutFile ts.exe
          .\ts.exe /quiet
          Start-Sleep 25
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TSKEY }} --hostname=rdp-lods
          & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          for ($i=0; $i -lt 360; $i++) { echo ONLINE $i; sleep 60 }
